#include <stdio.h>

int main() {

    int contador = 1;

    printf("Contando de 1 ate 5 usando WHILE:\n");

    while (contador <= 5) {
        printf("%d\n", contador);
        contador++;
    }

    return 0;
}
